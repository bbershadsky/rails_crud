<%= form_with(model: create_news, local: true) do |form| %>
  <% if create_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(create_news.errors.count, "error") %> prohibited this create_news from being saved:</h2>

      <ul>
      <% create_news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bill_to %>
    <%= form.text_field :bill_to, id: :create_news_bill_to %>
  </div>

  <div class="field">
    <%= form.label :contract_num %>
    <%= form.text_field :contract_num, id: :create_news_contract_num %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :create_news_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
